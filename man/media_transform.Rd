% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/media-transform.R
\name{media_transform}
\alias{media_transform}
\title{Adstock then saturate, in that order}
\usage{
media_transform(
  x,
  adstock = list(kernel = "geometric", decay = 0.5),
  saturation = list(type = "none"),
  by = NULL,
  order = c("adstock_then_saturate", "saturate_then_adstock")
)
}
\arguments{
\item{x}{Numeric vector of media spend in time order.}

\item{adstock}{A named list of arguments for the carryover step, including a
\code{kernel} element of \code{"geometric"} (the default), \code{"weibull"} or \code{"none"}.
Remaining elements are passed to \code{\link[=adstock_geometric]{adstock_geometric()}} or
\code{\link[=adstock_weibull]{adstock_weibull()}}.}

\item{saturation}{A named list of arguments for the saturation step,
including a \code{type} element of \code{"hill"}, \code{"exponential"},
\code{"michaelis_menten"}, \code{"power"} or \code{"none"}. Remaining elements are passed
to the corresponding \verb{saturate_*()} function -- for \code{"hill"} that means
\code{half_max}, which has no default because a sensible value depends entirely
on the scale of your spend.

Saturation defaults to \code{"none"}, so calling \code{media_transform()} with only
an \code{adstock} argument applies carryover alone.}

\item{by}{Optional grouping vector, or data frame of grouping vectors, the
same length as \code{x}.}

\item{order}{Transform order. \code{"adstock_then_saturate"} is the convention and
the default. \code{"saturate_then_adstock"} is permitted but warns, because it
is nearly always a mistake rather than a choice.}
}
\value{
A numeric vector the same length as \code{x}, in the same order.
}
\description{
Composes a carryover transform with a saturation curve to produce a single
model-ready regressor. The point of the function is the \emph{order}: adstock
first, saturation second.
}
\details{
The order is not arbitrary. Saturation represents a ceiling on what a given
weight of media can achieve in a period. Applying it before adstock caps each
period's spend in isolation and then lets carryover accumulate those capped
values past the cap, so the composed transform is no longer bounded by the
ceiling you specified. Applying it after adstock caps the \emph{total media
pressure} in each period, which is what a saturation curve is meant to mean.

This function will do it backwards if you insist, but it will not do it
backwards quietly.
}
\examples{
spend <- c(0, 500, 800, 300, 0, 0, 1200, 400)

# Carryover only -- saturation is off by default
round(media_transform(spend, adstock = list(decay = 0.5)), 4)

# Carryover and saturation together, in the conventional order
round(media_transform(
  spend,
  adstock = list(decay = decay_from_half_life(2)),
  saturation = list(half_max = 300)
), 4)

# A delayed-peak kernel with an S-shaped response
round(media_transform(
  spend,
  adstock = list(kernel = "weibull", shape = 2, scale = 2, max_lag = 6,
                 type = "pdf"),
  saturation = list(type = "hill", half_max = 300, shape = 2)
), 4)

# Grouped: geo-level panels transform within each geography
spend_panel <- c(100, 50, 25, 200, 100, 50)
geo <- rep(c("north", "south"), each = 3)
round(media_transform(spend_panel, adstock = list(decay = 0.5),
                      by = geo), 4)
}
\seealso{
\code{\link[=adstock_geometric]{adstock_geometric()}}, \code{\link[=saturate]{saturate()}}
}
