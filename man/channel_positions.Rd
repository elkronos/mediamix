% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path-diagnostics.R
\name{channel_positions}
\alias{channel_positions}
\title{Where each channel sits in the journey}
\usage{
channel_positions(paths, converting_only = TRUE)
}
\arguments{
\item{paths}{An \code{mm_paths} object from \code{\link[=build_paths]{build_paths()}}.}

\item{converting_only}{Restrict to converting journeys? Defaults to \code{TRUE},
which is what you want when reading these as a role description. Set
\code{FALSE} to see raw exposure.}
}
\value{
A data frame with one row per channel, ordered by descending
\code{touches}: \code{channel}, \code{only}, \code{first}, \code{middle}, \code{last}, \code{touches}, and
\code{first_last_ratio} (the count of first-touch appearances divided by the
count of last-touch appearances, \code{NA} when the channel never appears last).

This \code{first_last_ratio} is a ratio of touchpoint counts. The column of the
same name in \code{\link[=attribution_spread]{attribution_spread()}} is a ratio of credited shares; the two
will not agree numerically.
}
\description{
Counts how often each channel appears as the only touch, the first touch, a
middle touch or the last touch, and reports the ratio of first-touch to
last-touch appearances. A ratio well above 1 marks a channel that opens
journeys; well below 1 marks one that closes them.
}
\examples{
data(mm_events)
paths <- build_paths(mm_events, id = "customer_id", channel = "channel",
                     timestamp = "timestamp", conversion = "conversion")
channel_positions(paths)
}
\seealso{
\code{\link[=path_diagnostics]{path_diagnostics()}}, \code{\link[=assisted_conversions]{assisted_conversions()}}
}
