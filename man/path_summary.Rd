% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path-diagnostics.R
\name{path_summary}
\alias{path_summary}
\title{Journey table summary}
\usage{
path_summary(paths)
}
\arguments{
\item{paths}{An \code{mm_paths} object from \code{\link[=build_paths]{build_paths()}}.}
}
\value{
A one-row data frame with the columns
\describe{
\item{\code{events_in}, \code{events_out}}{Rows supplied, and rows retained.}
\item{\code{journeys}, \code{converting_journeys}}{Journeys built, and how many
ended in a conversion that can still be credited.}
\item{\code{mean_length}, \code{median_length}, \code{max_length}}{Journey length in
touchpoints.}
\item{\code{direct_events}}{Events whose channel matched \code{direct_labels}.}
\item{\code{conversion_events}}{Conversion \emph{events} in the input. This
exceeds \code{conversions_observed} when several conversions fall inside one
journey, which happens under \code{split_on = "none"} or \code{"gap"}.}
\item{\code{conversions_observed}}{Journeys that contained a conversion.}
\item{\code{conversions_unattributable}}{Converting journeys that lost every
touchpoint to a direct-traffic rule, and so cannot be credited to any
channel. That number should be reported, not absorbed.}
}

On a subset of a journey table the construction counts no longer apply, and
the columns describing the input are \code{NA} rather than the parent's values.
}
\description{
The construction accounting for an \code{mm_paths} object: how many events went
in, how many journeys came out, and how many conversions were lost to
filtering along the way.
}
\examples{
data(mm_events)
paths <- build_paths(mm_events, id = "customer_id", channel = "channel",
                     timestamp = "timestamp", conversion = "conversion")
path_summary(paths)
}
\seealso{
\code{\link[=build_paths]{build_paths()}}, \code{\link[=path_diagnostics]{path_diagnostics()}}
}
