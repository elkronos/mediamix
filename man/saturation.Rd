% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saturation.R
\name{saturation}
\alias{saturation}
\alias{saturate_hill}
\alias{saturate_exponential}
\alias{saturate_michaelis_menten}
\alias{saturate_power}
\alias{saturate}
\title{Saturation curves}
\usage{
saturate_hill(x, half_max, shape = 1)

saturate_exponential(x, rate)

saturate_michaelis_menten(x, vmax = 1, km)

saturate_power(x, exponent)

saturate(x, type = c("hill", "exponential", "michaelis_menten", "power"), ...)
}
\arguments{
\item{x}{Numeric vector of media, normally already adstocked. Values must be
non-negative: all four curves are defined for spend, not for arbitrary
reals.}

\item{half_max}{The value of \code{x} at which the response reaches half its
ceiling. Interpretable in the units of \code{x}, which is why it is used here in
preference to the equivalent unnamed scale parameter.}

\item{shape}{Hill exponent, positive. \code{shape = 1} gives a concave curve with
diminishing returns everywhere. \code{shape > 1} gives an S-shape with an
initial convex region, the usual representation of a threshold effect.}

\item{rate}{Exponential rate parameter, positive. Larger values saturate
sooner.}

\item{vmax}{Michaelis--Menten asymptote: the response as \code{x} grows without
bound.}

\item{km}{Michaelis--Menten constant: the value of \code{x} at which the response
reaches half of \code{vmax}.}

\item{exponent}{Power exponent in \verb{(0, 1]}. \code{exponent = 1} is the identity
(no saturation); smaller values bend the curve harder.}

\item{type}{Which curve \code{\link[=saturate]{saturate()}} should dispatch to.}

\item{...}{Passed to the individual curve function.}
}
\value{
A numeric vector the same length as \code{x}, in the same order.
}
\description{
Diminishing returns to media. Adstock says \emph{when} money works; saturation
says \emph{how hard} it works at the margin. Four curves are provided; they differ
in whether the response is bounded, and in whether they allow an S-shape.
}
\details{
\code{saturate_hill()} and \code{saturate_exponential()} are bounded on \verb{[0, 1)}, so
the fitted coefficient carries the channel's ceiling.
\code{saturate_michaelis_menten()} is bounded by \code{vmax}. All three approach their
ceiling smoothly at arbitrarily large \code{x}, including \code{Inf}, rather than
overflowing.
\code{saturate_power()} is unbounded but concave: response keeps growing, just
ever more slowly. Unboundedness is not automatically wrong, but it does mean
the model will happily extrapolate a return on a spend level never observed.

\code{saturate_hill(x, half_max, shape = 1)} and
\code{saturate_michaelis_menten(x, vmax = 1, km = half_max)} are the same
function. Both are provided because the two literatures name it differently
and practitioners arrive expecting one or the other.
}
\section{Transform order}{

Saturation is applied \emph{after} adstock, not before. Saturating first would
cap each period's spend in isolation and then let carryover accumulate the
capped values past the cap, which defeats the point of having a ceiling. Use
\code{\link[=media_transform]{media_transform()}} to get the order right without having to remember it.
}

\examples{
spend <- c(0, 25, 50, 100, 200, 400)

# Concave: diminishing returns from the first pound
round(saturate_hill(spend, half_max = 100), 3)

# S-shaped: a threshold below which media barely registers
round(saturate_hill(spend, half_max = 100, shape = 3), 3)

# The four curves side by side. Hill, exponential and Michaelis-Menten are
# bounded; power keeps growing, just ever more slowly.
round(rbind(
  hill        = saturate_hill(spend, half_max = 100),
  exponential = saturate_exponential(spend, rate = 0.007),
  michaelis   = saturate_michaelis_menten(spend, vmax = 1, km = 100),
  power       = saturate_power(spend, exponent = 0.5)
), 3)

# Bounded means bounded: no overflow, even at the extremes
saturate_hill(c(1e300, Inf), half_max = 100, shape = 3)

# The dispatcher, for programmatic use
round(saturate(spend, type = "hill", half_max = 100), 3)
}
\references{
Hill, A. V. (1910). The possible effects of the aggregation of the molecules
of haemoglobin on its dissociation curves. \emph{The Journal of Physiology},
40(Suppl), iv--vii.
}
\seealso{
\code{\link[=media_transform]{media_transform()}}, \code{\link[=response_curve]{response_curve()}}
}
