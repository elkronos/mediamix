% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path-diagnostics.R
\name{path_lengths}
\alias{path_lengths}
\title{Journey length distribution}
\usage{
path_lengths(paths)
}
\arguments{
\item{paths}{An \code{mm_paths} object from \code{\link[=build_paths]{build_paths()}}.}
}
\value{
A data frame with one row per journey length: \code{length}, \code{journeys},
\code{converting}, and \code{conversion_rate}. The conversion rate by length is worth
looking at before choosing a credit rule: if it is flat, extra touchpoints
are recording exposure rather than driving it.
}
\description{
Journey length distribution
}
\examples{
data(mm_events)
paths <- build_paths(mm_events, id = "customer_id", channel = "channel",
                     timestamp = "timestamp", conversion = "conversion")
path_lengths(paths)
}
\seealso{
\code{\link[=path_diagnostics]{path_diagnostics()}}
}
