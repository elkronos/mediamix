% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-ols.R
\name{fit_ols}
\alias{fit_ols}
\alias{predict.mm_ols}
\alias{print.mm_ols}
\title{Default linear fitter for carryover tuning}
\usage{
fit_ols(x, y)

\method{predict}{mm_ols}(object, newdata, ...)

\method{print}{mm_ols}(x, ...)
}
\arguments{
\item{x}{For \code{fit_ols()}, a numeric vector of transformed (adstocked)
media. For \code{print()}, an \code{mm_ols} object.}

\item{y}{Numeric vector of the response, the same length as \code{x}.}

\item{object}{An \code{mm_ols} object.}

\item{newdata}{Numeric vector of transformed media to predict from.}

\item{...}{Ignored, present for generic consistency.}
}
\value{
\code{fit_ols()} returns an object of class \code{mm_ols}: a list with elements
\code{intercept}, \code{slope} and \code{n}. \code{predict()} returns a numeric vector the same
length as \code{newdata}.
}
\description{
A bare intercept-and-slope least squares fit of \code{y} on transformed media,
supplied so that \code{\link[=tune_carryover]{tune_carryover()}} has a sensible default and the simple
case stays one line. It exists to be replaced: pass your own \code{fit_fn} and
\code{predict_fn} to tune carryover against the model you actually intend to fit.
}
\details{
This is deliberately the simplest possible model. It has no seasonality, no
price term, no baseline and no controls, so carryover parameters chosen
against it absorb whatever those omitted terms would have explained. That is
fine for a first pass and wrong for a deliverable.

To tune against a model with controls, note that \code{\link[=tune_carryover]{tune_carryover()}} hands
\code{fit_fn} only the adstocked media and the response -- there is no mechanism
for passing extra columns through. Two things follow: controls have to be
captured from the enclosing environment, and \code{predict_fn} has to supply them
for the assessment rows itself, which means knowing which rows those are.
Under a forward-only scheme the assessment rows are always the ones
immediately following the training rows, so a \code{predict_fn} can reconstruct
them from \code{length(newdata)} -- but it has to get that arithmetic exactly
right, and a silently misaligned control column produces a plausible number
rather than an error.

Two safer routes exist, and one of them is almost always what you want.
Residualise \code{y} against the controls first and tune carryover on the
residual, which needs no row bookkeeping at all; or use \code{\link[=step_adstock]{step_adstock()}} in a
\pkg{recipes} pipeline, where the resampling machinery keeps every row
aligned for you and carryover is tuned jointly with everything else. Both are
worked through in \code{vignette("carryover")}.
}
\examples{
set.seed(1)
spend <- adstock_geometric(c(100, 50, 0, 0, 200, 100, 0, 50), decay = 0.5)
kpi <- 10 + 0.4 * spend + rnorm(8, sd = 0.1)

m <- fit_ols(spend, kpi)
m
predict(m, spend)
}
\seealso{
\code{\link[=tune_carryover]{tune_carryover()}}
}
