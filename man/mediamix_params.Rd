% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dials-params.R
\name{mediamix_params}
\alias{mediamix_params}
\alias{carryover_decay}
\alias{carryover_max_lag}
\alias{saturation_half_max}
\alias{saturation_shape}
\title{Tuning parameters for media transforms}
\usage{
carryover_decay(range = c(0, 0.95), trans = NULL)

carryover_max_lag(range = c(1L, 26L), trans = NULL)

saturation_half_max(range = c(0.05, 1), trans = NULL)

saturation_shape(range = c(0.5, 3), trans = NULL)
}
\arguments{
\item{range}{A two-element vector giving the range of the parameter.}

\item{trans}{A transformation from \pkg{scales}, or \code{NULL} for none.}
}
\value{
A \code{dials} parameter object.
}
\description{
\pkg{dials} parameter objects for the quantities \code{\link[=step_adstock]{step_adstock()}} and
\code{\link[=step_saturation]{step_saturation()}} expose to tuning. Having these is the entire reason to be
a recipe step rather than a function you call beforehand: they let carryover
decay, saturation shape and a model's own penalty be tuned \emph{jointly}, in one
\code{tune::tune_bayes()} call over proper rolling-origin resampling, rather than
fixing the transform parameters by eye and tuning only the model.
}
\details{
\code{carryover_decay()} spans \verb{[0, 0.95]} rather than \verb{[0, 1)}. The upper bound
is a practical one: a decay of 0.99 implies a half-life of 69 periods, which
no ordinary media dataset can identify, and leaving it in the range mostly
wastes tuning iterations on parameter values that trade off against the
intercept.

\code{carryover_max_lag()} spans 1 to 26 periods. It is finite by necessity:
\code{Inf} is a legitimate value for \code{\link[=step_adstock]{step_adstock()}} but not a tunable one, since
a search cannot propose an infinite integer. So tuning \code{max_lag} over a
finite grid and leaving it fixed at \code{Inf} are genuinely different searches --
the first over truncated kernels, the second over the recursive one. Most of
the time, tuning \code{decay} with \code{max_lag = Inf} is the better use of the
budget: the truncation point is usually a modelling decision rather than
something the data speaks to.

\code{saturation_half_max()} is a fraction of each column's reference level, so
its range is unitless and comparable across channels. See
\code{\link[=step_saturation]{step_saturation()}}.

\code{saturation_shape()} spans \verb{[0.5, 3]}. Values above 1 give an S-shaped
response with a threshold below which media barely registers; below 1 the
curve bends harder than a plain hyperbola. Note that
\code{\link[=step_saturation]{step_saturation()}} narrows this range to \verb{(0.1, 1]} when
\code{type = "power"}, where \code{shape} is the exponent itself and a value above 1
would not be saturating at all.
}
\examples{
\dontshow{if (requireNamespace("dials", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
carryover_decay()
dials::value_seq(carryover_decay(), 5)

saturation_shape()
dials::grid_regular(carryover_decay(), saturation_shape(), levels = 3)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=step_adstock]{step_adstock()}}, \code{\link[=step_saturation]{step_saturation()}}
}
