% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attribute.R
\name{attribute}
\alias{attribute}
\title{Attribute conversions to channels under several rules at once}
\usage{
attribute(
  paths,
  rules = c("linear", "first", "last", "position", "time_decay"),
  first_weight = 0.4,
  last_weight = 0.4,
  decay = decay_from_half_life(7),
  period = 1
)
}
\arguments{
\item{paths}{An \code{mm_paths} object from \code{\link[=build_paths]{build_paths()}}.}

\item{rules}{Character vector of rules to apply: any of \code{"linear"},
\code{"first"}, \code{"last"}, \code{"position"} and \code{"time_decay"}.}

\item{first_weight, last_weight}{Passed to \code{\link[=credit_position]{credit_position()}}.}

\item{decay, period}{Passed to \code{\link[=credit_time_decay]{credit_time_decay()}}.}
}
\value{
A data frame with one row per rule and channel:
\describe{
\item{\code{rule}}{Which credit rule produced the row.}
\item{\code{channel}}{Channel label.}
\item{\code{conversions}}{Fractional conversions credited to the channel.}
\item{\code{share}}{\code{conversions} as a share of all conversions credited
under that rule. Shares sum to 1 within each rule.}
\item{\code{value}}{Credited conversion value, present only when the journey
table carries values.}
\item{\code{touches}}{Number of touchpoints on the channel across all
journeys, converting or not.}
}
Rows are ordered by rule, then by descending conversions.
}
\description{
Runs a set of credit rules over the same journey table and returns their
results stacked, so the spread between rules is visible rather than hidden by
a choice of default. That spread is the most useful output of rule-based
attribution: it bounds how much a channel's apparent value depends on the
convention rather than the data.
}
\examples{
data(mm_events)
paths <- build_paths(mm_events, id = "customer_id", channel = "channel",
                     timestamp = "timestamp", conversion = "conversion",
                     value = "value")

res <- attribute(paths)
head(res, 10)

# Total credited conversions equals observed converting journeys, per rule
tapply(res$conversions, res$rule, sum)

# How much does the answer depend on the rule?
attribution_spread(res)
}
\seealso{
\code{\link[=credit_linear]{credit_linear()}} and friends, \code{\link[=attribution_spread]{attribution_spread()}}
}
